import 'package:flutter/material.dart';

/// Placeholder — akan diisi lengkap di tahap pengerjaan berikutnya.
class AddSavingsScreen extends StatelessWidget {
  const AddSavingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tambah Target')),
      body: const Center(
        child: Padding(
          padding: EdgeInsets.all(24),
          child: Text(
            'Halaman "Tambah Target" akan dibangun di tahap berikutnya sesuai roadmap.',
            textAlign: TextAlign.center,
          ),
        ),
      ),
    );
  }
}
